# Diet Blog App

Simple diet blog app with Flask backend and Expo frontend.

## Run Backend
cd backend
pip install -r requirements.txt
python app.py

## Run Frontend
cd frontend
npm install
npx expo start

Update API URL in frontend files before running.
